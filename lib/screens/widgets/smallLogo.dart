/* import 'package:flutter/material.dart';
import 'package:nintendo_swt/screens/widgets/logo_left_white.dart';
import 'package:nintendo_swt/screens/widgets/logo_right_white.dart';

class SmallLogo extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Row(
      children: [
        LeftLogoWhite(
          height: MediaQuery.of(context).size.height * (47 / 667),
          width: MediaQuery.of(context).size.height * (22.7 / 667),
        ),
        RightLogoWhite(
          height: MediaQuery.of(context).size.height * (19 / 667),
          width: MediaQuery.of(context).size.height * (47 / 667),
          /* logo: true */
        ),
      ],
    );
  }
}
 */
